//Escribe una función que reciba tres números como parámetros, los sume y devuelva el resultado.
function suma3Numeros(x, y, z){
    let resultado = x + y + z;
    return resultado;
}
let suma = suma3Numeros(5, 2, 3)
console.log(suma)
alert(suma)